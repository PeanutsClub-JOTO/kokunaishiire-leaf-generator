-- ============================================================
-- AI生成背景URL
--
-- 完成PNGとは別にAI背景だけを保存し、ワークベンチの編集プレビューでも
-- 同じ背景を使えるようにする。
-- ============================================================

ALTER TABLE leaflets
  ADD COLUMN IF NOT EXISTS ai_background_url text;
