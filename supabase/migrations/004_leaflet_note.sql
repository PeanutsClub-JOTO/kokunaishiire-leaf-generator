-- ============================================================
-- リーフ単位のセールスコピー（note）
--
-- ワークベンチで編集したセールスコピーを永続化し、
-- リーフ画像生成時にも反映できるようにする。
-- 未設定時は商品(product)の note または自動生成にフォールバック。
-- ============================================================

ALTER TABLE leaflets
  ADD COLUMN IF NOT EXISTS note text;
